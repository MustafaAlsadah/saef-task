specVersion: "v1beta"
serviceId: "saef-task"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "saef-task-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
